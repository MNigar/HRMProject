@using HRProject.Extensions
@using HRProject.Models.DTO
@model bool?
@(Html.DevExtreme().DataGrid<ExcludedDayDTO>()
    .ID("dt-grid")
    .DataSource(d => d.Mvc().Area("Settings").Controller(ExcludedDayController.Name).LoadAction(nameof(ExcludedDayController.GetAll)))
    .RemoteOperations(true)
    .ShowBorders(true)
    .ShowRowLines(true)
    .AllowColumnResizing(true)
    .HRMGrid()
    .Columns(c =>
    {
        c.AddFor(x => x.HolidayDate).Caption(stringLocalizer["HolidayDate"]);
        c.AddFor(x => x.Description).Caption(stringLocalizer["Description"]);
    })
    .Pager(p => p.AllowedPageSizes(new int[] { 5, 20, 50 }))
    .Paging(paging => paging.PageSize(10))
.Selection(s => s.Mode(SelectionMode.Single)).OnSelectionChanged("dtGrid.selectionChanged")
)
